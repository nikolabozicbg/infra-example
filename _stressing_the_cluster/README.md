# Stress test the cluster with many post requests

## Setup

- Activate the python virtual environment with:
  `source bin/activate`

- To deactivate virtual environment in the same terminal:
  `deactivate`

## Prerequisites

Install dependencies:
`pip install -r requirements.txt`
